import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 1.0) {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, new color!!")
        }
    }
}
